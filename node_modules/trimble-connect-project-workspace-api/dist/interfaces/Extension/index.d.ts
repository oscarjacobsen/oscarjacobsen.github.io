/**
 * DISCLAIMER: This file contains the public interfaces and thus, upcoming changes should always be backwards-compatible.
 * It is only allowed to add new members.
 * DO NOT REMOVE existing members; instead, mark them as obsolete and create the new members.
 */
import { ExtensionAPI, ExtensionEvents } from "./ExtensionAPI";
import { ProjectAPI } from "./ProjectAPI";
import { UIAPI } from "./UIAPI";
import { UserAPI } from "./UserAPI";
export * from "./ExtensionAPI";
export * from "./ProjectAPI";
export * from "./UserAPI";
export * from "./UIAPI";
/** The API set which can be used to interact with the Trimble Connect Web.  */
export interface TCExtensionAPI {
    /** The API to interact with the Trimble Connect projects */
    project: ProjectAPI;
    /** The API to interact with the Trimble Connect users */
    user: UserAPI;
    /** The API to interact with the Trimble Connect Web UI */
    ui: UIAPI;
    /** The API to add and interact with Extensions */
    extension: ExtensionAPI;
}
/**
 * Connects to the Trimble Connect Web application.
 * @param target The target dispatcher or the iframe hosting the Trimble Connect Web application.
 * @param onEvent The callback that receives the events dispatched from the Trimble Connect Web application.
 * @param timeout The optional timeout in milliseconds. If not specified default timeout is 10000ms
 * @returns A promise that resolves to the set of exposed external API from the Trimble Connect Web application.
 *          Use the exposed API to communicate with the Trimble Connect Web application.
 */
export declare function connect(target: Window | HTMLIFrameElement, onEvent?: (event: ExtensionEvents, arg: {
    data: any;
}) => void, timeout?: number): Promise<TCExtensionAPI>;
