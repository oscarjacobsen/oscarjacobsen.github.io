/**
 * Divides an array into arrays with a given number of items, the last array contains the remaining items.
 * (similar to lodash.chunks but that caused compilation problems in consuming application)
 */
export declare function toChunks<T>(source: T[], chunkSize: number): T[][];
