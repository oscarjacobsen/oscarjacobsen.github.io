/**
 * Loads the translated strings.
 * @param values The translations.
 */
export declare function loadTranslations(values: unknown): void;
/**
 * Loads the translated strings from a file.
 * If file does not exist, en-US culture will be used as fall back.
 * @param culture The culture code.
 * @param path The path for the json file. This is optional. If this is not provides "locales/" will be used as default.
 */
export declare function loadTranslationsFromFile(culture: string, path?: string): Promise<void>;
/**
 * Returns the translated string.
 * @param key The translation key.
 */
export declare function translate(key: string): string;
/**
 * Loads draft texts that are not in translation database yet.
 * This makes it easy to use Translate method with draft texts
 * and having draft texts in one location in code.
 * @param values The draft keys and texts.
 */
export declare function addDraftTexts(values: Record<string, string>): void;
/**
 * Replaces the formatting tags in a localized text with their HTML tags.
 * Recognized tags are: [b], [/b], [nl/] (new line, becomes <br>), [a], [/a].
 * The [a] tags mark an anchor element. Right now only one anchor per text is supported.
 *  All [a] tags are replaced with the same anchor.
 *  If no anchor information is given in the parameter then all [a] tags are silently removed.
 */
export declare function parseRichTranslation(translation: string, anchor?: {
    target?: string;
    href: string;
}): string;
