/**
 * Debouncing timer.
 */
export declare class Debounce {
    /** The debouncing timer. */
    private timer;
    /**
     * Invokes an action.
     * @param milliseconds The milliseconds to wait before invoking the action.
     * @param action The action to invoke.
     */
    invoke(milliseconds: number, action: () => void): void;
    /**
     * Cancels the action invocation.
     */
    cancel(): void;
}
