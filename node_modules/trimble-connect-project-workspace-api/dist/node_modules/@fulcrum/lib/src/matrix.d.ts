declare interface Vector3 {
    x: number;
    y: number;
    z: number;
}
declare interface Matrix4 {
    elements: [number, number, number, 0, number, number, number, 0, number, number, number, 0, number, number, number, 1];
}
/**
 * The identity matrix.
 */
export declare const IDENTITY: Matrix4;
/**
 * Returns a transformation matrix.
 * @param x The X axis.
 * @param y The Y axis.
 * @param z The Z axis.
 * @param translate The translation vector.
 */
export declare function createTransform(x: Vector3, y: Vector3, z: Vector3, translate: Vector3): Matrix4;
export {};
