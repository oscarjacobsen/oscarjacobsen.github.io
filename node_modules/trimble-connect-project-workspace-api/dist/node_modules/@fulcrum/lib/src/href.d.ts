/** Query parameter map. */
export declare type IQueryParameters = Record<string, string | number | boolean | null | undefined>;
/**
 * Returns a formatted HREF.
 * @param baseUri The base URI.
 * @param path The path, including the leading slash.
 * @param query An object containing the query key-value pairs.
 */
export declare function formatHref(baseUri: string, path?: string, query?: IQueryParameters): string;
/**
 * Returns formatted query parameters.
 * @param query An object containing the query key-value pairs.
 */
export declare function formatQueryParameters(query: IQueryParameters): string;
/**
 * Returns a parsed query parameter or undefined if the HREF cound not be parsed.
 * @param hrefText The HREF to parse.
 * @param name The parameter name.
 */
export declare function parseQueryParameter(href: string, name: string): string | null | undefined;
/**
 * Returns true if 'env=stage' is present in the URL
 */
export declare const isStageEnv: (href: string) => boolean;
