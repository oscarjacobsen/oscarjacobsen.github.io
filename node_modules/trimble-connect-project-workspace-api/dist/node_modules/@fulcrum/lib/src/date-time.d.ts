/**
 * Returns the formatted date.
 * @param utcDateAndTime The UTC timestamp as a string.
 */
export declare function formatDate(utcDateAndTime: string, locale?: string): string;
/**
 * Returns the formatted date and time.
 * @param utcDateAndTime The UTC timestamp as a string.
 */
export declare function formatDateAndTime(utcDateAndTime: string | Date, locale?: string): string;
/**
 * Returns the date in YYYY-MM-DD format.
 * @param utcDateAndTime The UTC timestamp as a string.
 */
export declare function formatDateInvariant(utcDateAndTime: string): string;
/**
 * Returns a date from UTC timestamp.
 * @param utcDateAndTime The UTC date and time as a string.
 */
export declare function parseDate(utcDateAndTime: string): Date;
/**
 * Returns date and time from UTC timestamp.
 * @param utcDateAndTime The UTC date and time as a string.
 */
export declare function parseDateAndTime(utcDateAndTime: string): Date;
/** Returns the current UTC timestamp in seconds. */
export declare function utcTimestamp(): number;
