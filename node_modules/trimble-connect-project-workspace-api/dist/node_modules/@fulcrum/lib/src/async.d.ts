/**
 * Returns a promise that resolves after a delay.
 * @param milliseconds The milliseconds to delay.
 */
export declare function delay(milliseconds: number): Promise<{}>;
/** Returns a promise that will not resolve. */
export declare function forever<T>(): Promise<T>;
