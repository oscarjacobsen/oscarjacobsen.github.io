declare interface ColorRGB {
    r: number;
    g: number;
    b: number;
}
/**
 * Returns an RGB color formatted as a string.
 * @param color The color data.
 */
export declare function formatRGB(color: ColorRGB): string;
/**
 * Converts number to haxadecimal string.
 * @param number.
 */
export declare function toHex(v: number): string;
/**
 * Returns a parsed color from a string.
 * @param color The color string.
 */
export declare function parseRGB(color: string): ColorRGB;
export {};
